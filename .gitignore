# Windows / Google Drive junk — never part of the app
desktop.ini
Thumbs.db

# 3D map — shipped Sep 4 2026 (v.69.69.16): 3d.html and the sealed maps/3d/*.bundle* files are part of the app.

# Editor-only aerial tiles — NEVER deploy, even after the 3D go-ahead
*-aerial.pmtiles
